<%= tag.div class: "field block" do %>
  <%= tag.span class: listing_status_tag_classes(listing) do %>
    <strong>
      <%= listing.human_enum_value(:status) %>
    </strong>
  <% end %>
<% end unless listing.published? %>
